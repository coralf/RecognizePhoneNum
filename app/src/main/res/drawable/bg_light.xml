<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    >

    <!-- 填充的颜色 -->
    <solid android:color="@color/finder_laser_t" />

    <!-- 边框的颜色和粗细 -->
    <stroke
        android:width="1dp"
        android:color="@color/finder_laser_t"
        />
    <!-- android:radius 圆角的半径 -->
    <corners
        android:radius="5dp"
        />
</shape>